@model IEnumerable<OrderManagementSystem.Models.Product>

@{
    ViewData["Title"] = "Lista de Produtos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista de Produtos</h2>
<a href="/Products/Create" class="btn btn-primary mb-3">Novo Produto</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Preço</th>
            <th>Estoque</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var produto in Model)
        {
            <tr>
                <td>@produto.Name</td>
                <td>@produto.Price.ToString("C")</td>
                <td>@produto.Stock</td>
                <td>
                    <a href="/Products/Edit/@produto.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a href="/Products/Delete/@produto.Id" class="btn btn-danger btn-sm">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
